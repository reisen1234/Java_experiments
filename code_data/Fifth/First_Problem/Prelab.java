package code_data.Fifth.First_Problem;
//****************************************************************
// Prelab.java
//
// To make a boolean expression
//****************************************************************
public class Prelab {
    public static void main(String[] args)
    {
        int x = 3,y = 2,z = 1;
        boolean a,b,c,d;
        a = (x > y) && (y > z);
        b = x < 0 && y < 0;
        c = x > 0 && y > 0;
        d = (x == y) && (x != z);
    }
}